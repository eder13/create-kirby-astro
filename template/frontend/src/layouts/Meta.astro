---
import type { Meta } from '../types/seo/meta';

import EssentialMeta from './meta/EssentialMeta.astro';
import SEOMeta from './meta/SEOMeta.astro';

type Props = Meta;

const { title, ...rest } = Astro.props;
---

<>
    <EssentialMeta title={title} />
    <SEOMeta {...rest} />
</>
